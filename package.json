{
  "name": "coloring-page-maker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-tilesheet": "node scripts/generate-tilesheet.js",
    "generate-streamdeck": "python generate_ulanzi_profile.py",
    "dev": "npm run generate-tilesheet && npm run generate-streamdeck && vite",
    "build": "npm run generate-tilesheet && npm run generate-streamdeck && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "jspdf": "^3.0.3"
  }
}
